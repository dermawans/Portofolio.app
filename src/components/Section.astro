---
interface Props {
  id: string;
  title: string;
  eyebrow?: string;
}

const { id, title, eyebrow } = Astro.props;
---

<section
  id={id}
  class="scroll-mt-24 border-t border-surface-border/60 py-16 sm:py-20"
  aria-labelledby={`${id}-heading`}
>
  <div class="mx-auto max-w-5xl px-4 sm:px-6">
    {
      eyebrow && (
        <p class="mb-2 text-xs font-medium uppercase tracking-widest text-accent">
          {eyebrow}
        </p>
      )
    }
    <h2
      id={`${id}-heading`}
      class="mb-8 text-2xl font-semibold tracking-tight text-white sm:text-3xl"
    >
      {title}
    </h2>
    <slot />
  </div>
</section>
